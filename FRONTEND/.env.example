# Public backend API URL. Include the API version/prefix used by your backend.
# Vite embeds this value at build time, so set it in your hosting provider too.
VITE_API_BASE_URL=https://api.your-domain.example/api/v1
